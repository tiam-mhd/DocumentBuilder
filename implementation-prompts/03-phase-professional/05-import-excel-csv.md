# P03-T05 — Import Excel/CSV

## هدف
ورود گروهی داده (حداقل Projects) با mapping و preview قبل از commit.

## پرامپت اجرا

```
طبق Data Import عمل کن (Nest/Next/PG؛ فایل موقت در storage).

تسک: Import Excel/CSV را Full-Stack برای حداقل یک Entity (Projects) پیاده کن.

الزامات:
1) تحلیل + تسک‌لیست.
2) آپلود فایل → parse → mapping ستون‌ها → preview → commit تراکنشی.
3) خطاهای ردیفی واضح؛ EntitlementGuard؛ محدودیت حجم.
4) UI wizard؛ i18n؛ docs/api.
5) Redis job اگر فایل بزرگ است.

پایان: تموم شد یا تموم نشد + مرحله بعدی.
```

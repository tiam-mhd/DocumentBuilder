# P04-T06 — Analytics پایه

## هدف
آمار بازدید صفحه وب، دانلود PDF، کشور/دستگاه سطح پایه.

## پرامپت اجرا

```
طبق Analytics عمل کن؛ حریم خصوصی را رعایت کن (بدون PII غیرضروری).

تسک: Analytics پایه را Full-Stack پیاده کن.

الزامات:
1) تحلیل + تسک‌لیست؛ ذخیره در PG یا time-series ساده.
2) رویداد view/download؛ داشبورد Owner.
3) بدون بلوکه کردن request path — async از طریق صف Redis ترجیحی.
4) docs؛ i18n؛ Opt-out اگر لازم مستند شود.

پایان: تموم شد یا تموم نشد + مرحله بعدی.
```
